{
  "projects": [
    {
      "id": "kafka",
      "title": "Real-Time IoT Anomaly Detection Pipeline",
      "status": "live",
      "value": "Detects equipment anomalies in real time from streaming sensor data, before they become costly failures.",
      "icon": "kafka",
      "media": { "type": "image",     "url": "https://raw.githubusercontent.com/mohammed-taha-el-ahmar/iot-anomaly-pipeline/main/docs/img/iot-anomalie-pipeline-demo.gif" },
      "architecture": "Sensor simulator → Kafka (partitioned by device) → Spark Structured Streaming → Delta Lake (bronze / silver / gold) → Alert dashboard",
      "decisions": [
        "Kafka over a simpler queue — needed ordered delivery per device and the ability to replay historical streams for backtesting alert thresholds.",
        "Structured Streaming over batch — sub-minute detection latency is the entire point; a 15-minute batch window would defeat the purpose.",
        "Medallion architecture in Delta Lake — raw events preserved untouched in bronze for replay/debugging, cleaned in silver, aggregated alerts in gold."
      ],
      "tags": ["Kafka", "Spark Structured Streaming", "Delta Lake", "Python", "Docker Compose"],
      "github": "https://github.com/mohammed-taha-el-ahmar/iot-anomaly-pipeline"
    },
    {
      "id": "airflow",
      "title": "Self-Healing Daily Pipeline with Airflow",
      "status": "live",
      "value": "A daily ingestion pipeline that catches bad data before it reaches dashboards — and explains exactly what broke and why.",
      "icon": "airflow",
      "media": { "type": "image",     "url": "https://raw.githubusercontent.com/mohammed-taha-el-ahmar/self-healing-pipeline/main/docs/img/demo-self-healing-pipeline.gif" },
      "architecture": "Source API → Airflow DAG (extract → validate → load) → Data quality gate (pass → warehouse table / fail → quarantine + Slack alert)",
      "decisions": [
        "Airflow over cron — needed dependency management, automatic retries with backoff, and the ability to backfill any historical date on demand.",
        "Quarantine on failure, not fail-fast — bad batches are isolated for inspection rather than blocking the whole pipeline, so good data still flows.",
        "Idempotent task design — every run for a given date can be safely re-executed without creating duplicates, which is what makes backfills safe."
      ],
      "tags": ["Apache Airflow", "Python", "Great Expectations", "Postgres", "Docker Compose"],
      "github": "https://github.com/mohammed-taha-el-ahmar/self-healing-pipeline"
    },
    {
      "id": "dbt",
      "title": "Analytics Engineering with dbt + Snowflake",
      "status": "live",
      "value": "Transforms raw operational data into trusted, tested business metrics — with full lineage so any number can be traced back to its source.",
      "icon": "dbt",
      "media": { "type": "image",     "url": "https://raw.githubusercontent.com/mohammed-taha-el-ahmar/analytics-engineering-dbt/main/docs/img/dbt-snowflake-demo.gif" },
      "architecture": "Raw source tables → Snowflake → dbt (staging → intermediate → marts) → tested, documented models → BI tool",
      "decisions": [
        "Layered modeling (staging / intermediate / marts) — keeps raw-to-business-logic transformations traceable and each layer independently testable.",
        "dbt tests as the contract — not_null, relationships, and custom business logic tests run on every build, so broken assumptions fail loudly, not silently in a dashboard.",
        "Incremental models for high-volume tables — avoids full table rebuilds, keeping both cost and run time bounded as data grows."
      ],
      "tags": ["dbt", "Snowflake", "SQL", "GitHub Actions"],
      "github": "https://github.com/mohammed-taha-el-ahmar/analytics-engineering-dbt"
    },
    {
      "id": "dbt-nl-sql-agent",
      "title": "NL-to-SQL Agent over dbt Marts",
      "status": "live",
      "value": "Lets anyone query trusted dbt marts in plain English — the agent grounds itself in dbt's own schema metadata, validates every query before it touches a database, and self-corrects when it gets the SQL wrong.",
      "icon": "agent",
      "media": { "type": "image", "url": "https://raw.githubusercontent.com/mohammed-taha-el-ahmar/analytics-engineering-dbt/dbt-nl-sql-agent/docs/img/dbt-nl-sql-agent-demo.gif" },
      "architecture": "Plain-English question → Groq agent → SQL generated against dbt manifest schema → sqlglot guard (read-only, catalog-bound, row-limited) → DuckDB demo / Snowflake prod → self-correction on failure → result + attempt trail",
      "decisions": [
        "Schema grounding from dbt's own manifest.json, not a hand-maintained file — the agent's vocabulary can never drift further from reality than the last `dbt docs generate`.",
        "Read-only SQL guard via sqlglot — every candidate query is parsed and rejected unless it's a single SELECT against catalog-approved tables, before it ever reaches a database.",
        "Bounded self-correction loop — failed validation or execution feeds the real error back to the model for up to 3 attempts, surfaced in the UI as an attempt trail rather than hidden behind the final answer."
      ],
      "tags": ["dbt", "Snowflake", "DuckDB", "Groq", "FastAPI", "Agentic AI"],
      "github": "https://github.com/mohammed-taha-el-ahmar/analytics-engineering-dbt/dbt-nl-sql-agent"
    },
    {
      "id": "aws-data-pipeline",
      "title": "Serverless Data Lakehouse on AWS",
      "status": "live",
      "value": "Ingests a live API into a queryable S3 data lake — Glue handles schema and transformation, Redshift Spectrum queries the lake directly with no load step.",
      "icon": "aws",
      "media": { "type": "image", "url": "https://raw.githubusercontent.com/mohammed-taha-el-ahmar/aws-data-pipeline/main/docs/img/aws-data-pipeline-demo.gif" },
      "architecture": "API → Lambda (ingest) → S3 raw/ → Glue Crawler → Glue Catalog → Glue ETL (Spark) → S3 processed/ (Parquet) → Glue Crawler → Redshift Spectrum → API Gateway → Front",
      "decisions": [
        "Right tool per stage — Lambda for cheap scheduled ingestion, Glue (Spark) for bulk transformation and schema management. Each service does what it's good at, not duplicating the other.",
        "Glue Workflow for orchestration — crawl(raw) → transform → crawl(processed) chained with conditional triggers, so the pipeline is decoupled from Lambda and fully Glue-native.",
        "Redshift Spectrum over the Glue Catalog — the warehouse queries Parquet in S3 directly as external tables, avoiding a COPY/load step and any Glue↔Redshift networking, with IAM as the only integration surface.",
        "Terraform for the whole stack — S3, Lambda, Glue (catalog + crawlers + job + workflow), Redshift Serverless and its Spectrum IAM role all provisioned and destroyed from one IaC tree, keeping cost bounded between demos."
      ],
      "tags": ["AWS", "Lambda", "Glue", "Redshift Spectrum", "S3", "Terraform", "Python"],
      "github": "https://github.com/mohammed-taha-el-ahmar/aws-data-pipeline"
    },
    {
      "id": "gcp-data-pipeline",
      "title": "Event-Driven Pipeline on GCP",
      "status": "planned",
      "value": "Streams live API data through Cloud Functions into BigQuery — fully serverless, with Eventarc wiring so each stage triggers the next without a scheduler in between.",
      "icon": "gcp",
      "media": { "type": "image", "url": "https://raw.githubusercontent.com/mohammed-taha-el-ahmar/gcp-data-pipeline/main/docs/img/gcp-architecture.jpg" },
      "architecture": "API → Cloud Scheduler → Cloud Function (ingest) → Cloud Storage raw/ → Eventarc trigger → Cloud Function (transform) → BigQuery → Looker Studio / Front",
      "decisions": [
        "Cloud Functions Gen2 over Cloud Run — the workload is small and bursty; Gen2 gives Eventarc integration and a 10-min timeout without the container-management overhead of Cloud Run.",
        "Eventarc (object-finalize) for stage coupling — the transform function fires on new objects in GCS, so ingestion and transformation are decoupled; either can be replaced or retried independently.",
        "BigQuery streaming inserts from the transform function — avoids a load job for single-row writes, keeping latency low at this scale while still landing data in a warehouse that supports full SQL and easy BI tool integration.",
        "Terraform for the full stack — GCS buckets, both Cloud Functions, BigQuery dataset + table, Cloud Scheduler job, all provisioned and destroyed from one configuration."
      ],
      "tags": ["GCP", "Cloud Functions", "BigQuery", "Cloud Storage", "Eventarc", "Terraform", "Python"],
      "github": "https://github.com/mohammed-taha-el-ahmar/gcp-data-pipeline"
    },
    {
      "id": "azure-data-pipeline",
      "title": "ADF-Orchestrated Pipeline on Azure",
      "status": "in_progress",
      "value": "Uses Azure Data Factory as the orchestration backbone — a Copy Activity lands raw data, an Azure Function transforms it, and Azure SQL serves it, showing the native Azure integration pattern most teams actually use.",
      "icon": "azure",
      "media": { "type": "image", "url": "https://raw.githubusercontent.com/mohammed-taha-el-ahmar/azure-data-pipeline/main/docs/img/azure-architecture.jpg" },
      "architecture": "API → ADF Pipeline (Copy Activity) → ADLS Gen2 raw/ → Blob trigger → Azure Function (transform) → Azure SQL Database (serverless) → Power BI / Front",
      "decisions": [
        "Data Factory as the entry point — ADF's Copy Activity handles REST-to-ADLS ingestion natively, so the ingest step is a managed service rather than custom code, which is the idiomatic Azure pattern.",
        "ADLS Gen2 with hierarchical namespace — enables both blob-trigger wiring for the Azure Function and future compatibility with Synapse/Databricks without migrating storage.",
        "Azure SQL serverless with auto-pause — the warehouse costs nothing when idle (auto-pauses after 60 min), making it demo-safe while still being a real relational engine with full T-SQL support.",
        "Terraform for infrastructure, ADF authoring UI for pipeline logic — the pipeline JSON skeleton is committed for reference, but linked services and datasets are created in the ADF UI where they're testable, matching how most teams actually work with Data Factory."
      ],
      "tags": ["Azure", "Data Factory", "ADLS Gen2", "Azure Functions", "Azure SQL", "Terraform", "Python"],
      "github": "https://github.com/mohammed-taha-el-ahmar/azure-data-pipeline"
    },
    {
      "id": "k8s-airflow-data-platform",
      "title": "Airflow on Kubernetes — Orchestration Capstone",
      "status": "live",
      "value": "Runs the same ingest + transform logic as the cloud-native projects, but you own the orchestration layer — showing when and why you'd choose a portable scheduler over vendor-specific triggers.",
      "icon": "kubernetes",
      "media": { "type": "image", "url": "https://raw.githubusercontent.com/mohammed-taha-el-ahmar/k8s-airflow-data-platform/main/docs/img/k8s-airflow-data-platform-demo.gif" },
      "architecture": "Airflow (Helm on K8s) → Task: ingest pod → object storage → Task: transform pod → warehouse",
      "decisions": [
        "kind/minikube over managed K8s — demonstrates the same Airflow-on-Kubernetes pattern without EKS/GKE/AKS cost; the Helm chart and manifests are portable to any cluster.",
        "Same shared/ module as the cloud repos — the ingest and transform logic is identical, making the architectural comparison honest: only the orchestration and deployment layer changes.",
        "PythonOperator first, KubernetesPodOperator as an extension point — starts simple, with a clear upgrade path to per-task isolation in its own pod for heavier workloads.",
        "Warehouse-agnostic by design — the DAG can load into local Postgres (via the docker/ stack), or any of the three cloud warehouses via credentials passed as Kubernetes secrets."
      ],
      "tags": ["Kubernetes", "Airflow", "Helm", "Docker", "Python"],
      "github": "https://github.com/mohammed-taha-el-ahmar/k8s-airflow-data-platform"
    }
  ]
}
